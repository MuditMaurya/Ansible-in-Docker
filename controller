FROM alpine:latest

RUN apk update \
    && apk add openssh ansible vim \
    && mkdir /authorized_keys \
    && mkdir /etc/ansible \
    && touch /etc/ansible/hosts \
    && ssh-keygen -f ~/.ssh/id_rsa -N ''
